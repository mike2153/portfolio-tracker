# Debug JSON Response Feature
# Set to 'true' to enable JSON debugging (logs to console)
NEXT_PUBLIC_DEBUG_JSON=false

# Set to 'true' to also save JSON responses as downloadable files
NEXT_PUBLIC_DEBUG_JSON_SAVE=false

# Example usage:
# 1. Copy this file to .env.local
# 2. Set NEXT_PUBLIC_DEBUG_JSON=true to enable console logging
# 3. Set NEXT_PUBLIC_DEBUG_JSON_SAVE=true to auto-download JSON files
# 4. Load a stock in the research page
# 5. Click on Financials tab to capture financial data
# 6. JSON files will be downloaded automatically with format:
#    - {TICKER}_stock-research_{timestamp}.json
#    - {TICKER}_financials-income_{timestamp}.json
#    - {TICKER}_financials-balance_{timestamp}.json
#    - {TICKER}_financials-cashflow_{timestamp}.json